from models import Product, Category
from django.core.context_processors import request


'''def my_breadcrumbs(request):
    if object.category:



    categories = []

    def path(category):
        categories.append(category)
        if category.parent:
            path(category.parent)
    path(object)
    return categories[::-1]'''